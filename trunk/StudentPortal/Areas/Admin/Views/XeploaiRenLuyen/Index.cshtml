@model IEnumerable<StudentPortal.STU_XeploaiRenLuyen>

@{
    ViewBag.Title = "Index";
	Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="row-fluid">
	<div class="span12">

		<h2>Index</h2>

<p>
    @Html.ActionLink("Thêm", "Create", new { }, new { @class = "btn green" })
</p>
@(Html.Kendo().Grid<StudentPortal.STU_XeploaiRenLuyen>()
	.Name("STU_XeploaiRenLuyen")
	.Columns(columns =>
	{
			columns.Bound(model => model.Xep_loai);
				columns.Bound(model => model.Tu_diem);
				columns.Bound(model => model.Den_diem);
				columns.Bound(model => model.He_so);
		        columns.Command(command => {command.Custom("Sửa").Click("Edit"); command.Custom("Xóa").Click("Delete");});
	})
	.Groupable()
	.Pageable()
	.Sortable()
	.DataSource(
		dataSource => dataSource.Ajax()
			.Read(read => read.Action("Read", "XeploaiRenLuyen"))
	)
)

	</div>
</div>
<script type="text/javascript">
	function Edit(e) {
		e.preventDefault();
		var dataItem = this.dataItem($(e.currentTarget).closest('tr'));
		location.href='@Url.Action("Edit","XeploaiRenLuyen")/'+dataItem.ID_xep_loai;
	}
	function Delete(e) {
		e.preventDefault();
		var dataItem = this.dataItem($(e.currentTarget).closest('tr'));
		location.href = '@Url.Action("Delete","XeploaiRenLuyen")/' + dataItem.ID_xep_loai;
	}
</script>
