@model IEnumerable<StudentPortal.STU_NhomDoiTuong>

@{
    ViewBag.Title = "Index";
	Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="row-fluid">
	<div class="span12">

		<h2>Index</h2>

<p>
    @Html.ActionLink("Thêm", "Create", new { }, new { @class = "btn green" })
</p>
@(Html.Kendo().Grid<StudentPortal.STU_NhomDoiTuong>()
	.Name("STU_NhomDoiTuong")
	.Columns(columns =>
	{
			columns.Bound(model => model.Ma_nhom);
				columns.Bound(model => model.Ten_nhom);
				columns.Bound(model => model.Ten_nhom_en);
		        columns.Command(command => {command.Custom("Sửa").Click("Edit"); command.Custom("Xóa").Click("Delete");});
	})
	.Groupable()
	.Pageable()
	.Sortable()
	.DataSource(
		dataSource => dataSource.Ajax()
			.Read(read => read.Action("Read", "NhomDoiTuong"))
	)
)

	</div>
</div>
<script type="text/javascript">
	function Edit(e) {
		e.preventDefault();
		var dataItem = this.dataItem($(e.currentTarget).closest('tr'));
		location.href='@Url.Action("Edit","NhomDoiTuong")/'+dataItem.ID_nhom_dt;
	}
	function Delete(e) {
		e.preventDefault();
		var dataItem = this.dataItem($(e.currentTarget).closest('tr'));
		location.href = '@Url.Action("Delete","NhomDoiTuong")/' + dataItem.ID_nhom_dt;
	}
</script>
