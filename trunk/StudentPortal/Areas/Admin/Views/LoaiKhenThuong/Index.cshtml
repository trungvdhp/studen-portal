@model IEnumerable<StudentPortal.STU_LoaiKhenThuong>

@{
    ViewBag.Title = "Index";
	Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="row-fluid">
	<div class="span12">

		<h2>Index</h2>

<p>
    @Html.ActionLink("Thêm", "Create", new { }, new { @class = "btn green" })
</p>
@(Html.Kendo().Grid<StudentPortal.STU_LoaiKhenThuong>()
	.Name("STU_LoaiKhenThuong")
	.Columns(columns =>
	{
			columns.Bound(model => model.STU_CapKhenThuongKyLuat.Ten_cap);
				columns.Bound(model => model.Loai_khen_thuong);
				columns.Bound(model => model.Diem_thuong);
		        columns.Command(command => {command.Custom("Sửa").Click("Edit"); command.Custom("Xóa").Click("Delete");});
	})
	.Groupable()
	.Pageable()
	.Sortable()
	.DataSource(
		dataSource => dataSource.Ajax()
			.Read(read => read.Action("Read", "LoaiKhenThuong"))
	)
)

	</div>
</div>
<script type="text/javascript">
	function Edit(e) {
		e.preventDefault();
		var dataItem = this.dataItem($(e.currentTarget).closest('tr'));
		location.href='@Url.Action("Edit","LoaiKhenThuong")/'+dataItem.ID_loai_kt;
	}
	function Delete(e) {
		e.preventDefault();
		var dataItem = this.dataItem($(e.currentTarget).closest('tr'));
		location.href = '@Url.Action("Delete","LoaiKhenThuong")/' + dataItem.ID_loai_kt;
	}
</script>
