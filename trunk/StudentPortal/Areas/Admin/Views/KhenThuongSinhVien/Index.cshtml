@model IEnumerable<StudentPortal.STU_KhenThuongSinhVien>

@{
    ViewBag.Title = "Index";
	Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="row-fluid">
	<div class="span12">

		<h2>Index</h2>

<p>
    @Html.ActionLink("Thêm", "Create", new { }, new { @class = "btn green" })
</p>
@(Html.Kendo().Grid<StudentPortal.STU_KhenThuongSinhVien>()
	.Name("STU_KhenThuongSinhVien")
	.Columns(columns =>
	{
			columns.Bound(model => model.STU_HoSoSinhVien.Ma_sv);
		        columns.Command(command => {command.Custom("Sửa").Click("Edit"); command.Custom("Xóa").Click("Delete");});
	})
	.Groupable()
	.Pageable()
	.Sortable()
	.DataSource(
		dataSource => dataSource.Ajax()
			.Read(read => read.Action("Read", "KhenThuongSinhVien"))
	)
)

	</div>
</div>
<script type="text/javascript">
	function Edit(e) {
		e.preventDefault();
		var dataItem = this.dataItem($(e.currentTarget).closest('tr'));
		location.href='@Url.Action("Edit","KhenThuongSinhVien")/'+dataItem.ID_khen_thuong;
	}
	function Delete(e) {
		e.preventDefault();
		var dataItem = this.dataItem($(e.currentTarget).closest('tr'));
		location.href = '@Url.Action("Delete","KhenThuongSinhVien")/' + dataItem.ID_khen_thuong;
	}
</script>
