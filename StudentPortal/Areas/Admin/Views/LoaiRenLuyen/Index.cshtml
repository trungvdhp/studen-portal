@model IEnumerable<StudentPortal.STU_LoaiRenLuyen>

@{
    ViewBag.Title = "Index";
	Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="row-fluid">
	<div class="span12">

		<h2>Index</h2>

<p>
    @Html.ActionLink("Thêm", "Create", new { }, new { @class = "btn green" })
</p>
@(Html.Kendo().Grid<StudentPortal.STU_LoaiRenLuyen>()
	.Name("STU_LoaiRenLuyen")
	.Columns(columns =>
	{
			columns.Bound(model => model.STU_CapRenLuyen.Ky_hieu);
				columns.Bound(model => model.Ky_hieu);
				columns.Bound(model => model.Ten_loai);
				columns.Bound(model => model.Diem);
		        columns.Command(command => {command.Custom("Sửa").Click("Edit"); command.Custom("Xóa").Click("Delete");});
	})
	.Groupable()
	.Pageable()
	.Sortable()
	.DataSource(
		dataSource => dataSource.Ajax()
			.Read(read => read.Action("Read", "LoaiRenLuyen"))
	)
)

	</div>
</div>
<script type="text/javascript">
	function Edit(e) {
		e.preventDefault();
		var dataItem = this.dataItem($(e.currentTarget).closest('tr'));
		location.href='@Url.Action("Edit","LoaiRenLuyen")/'+dataItem.ID_loai_rl;
	}
	function Delete(e) {
		e.preventDefault();
		var dataItem = this.dataItem($(e.currentTarget).closest('tr'));
		location.href = '@Url.Action("Delete","LoaiRenLuyen")/' + dataItem.ID_loai_rl;
	}
</script>
