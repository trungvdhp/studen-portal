@model IEnumerable<StudentPortal.STU_LoaiGiayTo>

@{
    ViewBag.Title = "Index";
	Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="row-fluid">
	<div class="span12">

		<h2>Index</h2>

<p>
    @Html.ActionLink("Thêm", "Create", new { }, new { @class = "btn green" })
</p>
@(Html.Kendo().Grid<StudentPortal.STU_LoaiGiayTo>()
	.Name("STU_LoaiGiayTo")
	.Columns(columns =>
	{
			columns.Bound(model => model.Ma_giay_to);
				columns.Bound(model => model.Ten_giay_to);
				columns.Bound(model => model.So_luong);
				columns.Bound(model => model.An);
		        columns.Command(command => {command.Custom("Sửa").Click("Edit"); command.Custom("Xóa").Click("Delete");});
	})
	.Groupable()
	.Pageable()
	.Sortable()
	.DataSource(
		dataSource => dataSource.Ajax()
			.Read(read => read.Action("Read", "LoaiGiayTo"))
	)
)

	</div>
</div>
<script type="text/javascript">
	function Edit(e) {
		e.preventDefault();
		var dataItem = this.dataItem($(e.currentTarget).closest('tr'));
		location.href='@Url.Action("Edit","LoaiGiayTo")/'+dataItem.ID_giay_to;
	}
	function Delete(e) {
		e.preventDefault();
		var dataItem = this.dataItem($(e.currentTarget).closest('tr'));
		location.href = '@Url.Action("Delete","LoaiGiayTo")/' + dataItem.ID_giay_to;
	}
</script>
